#include <stdio.h>
#include <stdlib.h>
#include "dlist.h"

int main(void)
{

	/*int a = 1;
	int b = 2;
	int c = 3;

	struct dlist_ *list = (struct dlist_ *) malloc (sizeof (struct dlist_));

	insert(list, &a, 0);
	insert(list, &b, 0);
	insert(list, &c, 0);*/

	return 0;
}
